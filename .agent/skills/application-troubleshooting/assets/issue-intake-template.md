Subject: [Bug] <short title> — <environment/region>

What happened:
- Symptom summary (error text, crash, incorrect result, slowness).
- Expected vs actual behavior.

Scope:
- Users/tenants/regions affected (estimate % if known).
- First seen (timestamp + timezone) and frequency (always/intermittent).
- Routes/screens/endpoints involved.

Environment:
- App version/commit or build number.
- Feature flags/config values relevant to the area.
- Device/OS/Browser (version) or service host/region.

Reproduction steps:
1)
2)
3)

Inputs/artifacts:
- Request/trace IDs, sample payloads, screenshots, console/network log snippets.
- Any toggles or special conditions required (e.g., signed-out user, specific account).

Recent changes:
- Deploys/config/flag flips/migrations/dependency bumps near first-seen time.

Impact/risk:
- Data writes? risk of corruption? payments? security/privacy concern?

Owner & next checkpoint:
- Primary responder + communication cadence (e.g., updates every 30m) and rollback path.
